/**
 * LLM Module (주석처리 - RAG로 대체)
 * LlmService가 주석처리되어 이 모듈도 사용하지 않습니다.
 */
/*
import { Module } from '@nestjs/common';
import { LlmService } from './llm.service';

@Module({
    providers: [LlmService],
    exports: [LlmService],
})
export class LlmModule {}
*/
